pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
               docker { image 'docker-local.artifactory.tools.vsware.ie/ci-runner-python-build-container' }
            }
            steps {
                  sh 'python --version'
                  sh 'pip --version'
            }
        }
    }
}

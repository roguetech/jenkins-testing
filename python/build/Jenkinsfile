pipeline {
    agent {
        docker {
            image 'docker-local.artifactory.tools.vsware.ie/ci-runner-python-build-container:1.0.8'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh ( 
                  script: """ #!/usr/bin/bash
                  python --version
                  pip --version
                  ls -la /opt/rh/rh-python36/
                  """
                )
            }
        }
    }
}
